# Assets públicos

Coloca aquí archivos servidos en la raíz de la app.

- `nfc.png` — icono NFC global (`ICONO_NFC_URL` apuntará a `https://TU_DOMINIO/nfc.png`)

Tras desplegar en Cloudflare Pages, la URL será:

```text
https://<tu-proyecto>.pages.dev/nfc.png
```
